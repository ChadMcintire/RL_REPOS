defaults:
  - model
  - training
  - render
  - env
  - algo/ppo
  - eval

hydra:
  run:
    dir: ../outputs/logs/${now:%Y-%m-%d}/${now:%H-%M-%S}

paths:
  video_dir: ../outputs/video
  log_dir: ../outputs/logs
